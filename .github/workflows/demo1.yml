name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome Message
          run: echo "My First Github Actions" 
        
        - name: List Files
          run: ls
        - name: Read file
          run: cat one.js